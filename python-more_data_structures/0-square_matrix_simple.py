#!/usr/bin/python3
def square_matrix_simple(matrix=[]):
    new_list = []
    for x, in enumerate(matrix):
        new_list[x]
        for y, j in enumerate(i):
            new_list[x][y] = j
    print(new_list)
